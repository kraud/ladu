{
  "name": "keelapp-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Ladu — full-stack Playwright end-to-end suite (per-phase gate verification)",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui --headed",
    "test:smoke": "playwright test --config=playwright.deploy.config.ts",
    "report": "playwright show-report",
    "install:browsers": "playwright install chromium",
    "stub:oidc": "tsx fixtures/oidc-stub/server.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^20.14.0",
    "@types/pg": "^8.11.0",
    "dotenv": "^16.4.0",
    "jose": "^6.2.12",
    "pg": "^8.11.0",
    "tsx": "^4.21.0"
  }
}
