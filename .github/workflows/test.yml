name: Test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  backend:
    name: Backend (Jest + PostgreSQL)
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: keelapp_user
          POSTGRES_PASSWORD: keelapp_password
          POSTGRES_DB: keelapp_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U keelapp_user"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      NODE_ENV: test
      TEST_DATABASE_URL: postgresql://keelapp_user:keelapp_password@localhost:5432/keelapp_test
      JWT_SECRET: test_secret

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm test -w backend

  frontend:
    name: Frontend (Vitest + build)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run test -w frontend
      - run: npm run build -w frontend
