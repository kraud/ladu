---
- name: Configure the Ladu VPS
  hosts: vps
  gather_facts: true
  become: true
  roles:
    - base
    - firewall
    - docker
  # Remaining role, added in the next slice:
  #   platform  - /opt/ladu dirs, platform.yml up, DBs/users, log rotation,
  #               and the DOCKER-USER firewall fix for published ports
